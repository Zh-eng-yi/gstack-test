// #include "internal/gstack.h"
#include <stdlib.h>
#include <stdbool.h>
#include <mprompt.h>
#include <stdio.h>
#include <string.h>
#include <stdint.h>

int main() {
    /*
    printf("allocating gstack...\n");
    mp_gstack_t* g = mp_gstack_alloc(0, NULL); 

    printf("stack_size: %zd\n", g->stack_size);

    // printf("writing to stack...\n");
    // uint8_t *stk = g->stack;
    // memset(stk, 0, 10);

    char *stk = (char *) g->stack;

    printf("at -1: %c\n", stk[-1]);

    printf("check committed size:\n");
    printf("initial_commit: %zd\n", g->initial_commit);
    printf("committed: %zd\n", g->committed);

    printf("freeing gstack...\n");
    mp_gstack_free(g, false);
    */
}